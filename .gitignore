node_modules/
dist/
*.log
.env
.env.*
!.env.example
# Vite convention: .env.development holds non-secret dev defaults and is committed
# (unlike .env*.local). The client dev server URL lives here.
!client/.env.development
.DS_Store
Thumbs.db
.superpowers/

# 갤러리 원본 사진 (웹용 변환본만 client/public/gallery에 커밋)
mypictures/
